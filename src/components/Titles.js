import React from "react";

const Titles = props => (
  <div>
    <p className="title-container__title">Weather Finder</p>
    <p className="title-container__subtitle">Find out temperature, conditions and more...</p>
  </div>
);

export default Titles;
